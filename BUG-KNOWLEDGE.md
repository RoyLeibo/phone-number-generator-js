date,description,root_cause,fix_summary,avoid
2026-03-24,ESM build fails with ERR_MODULE_NOT_FOUND for relative imports,TypeScript preserves extensionless relative imports in ESM output; Node ESM requires explicit .js extensions,Added .js to all relative imports in src/ + moduleNameMapper in jest.config.js + extensionAlias in webpack.config.js,Always use .js extensions in TypeScript source relative imports when targeting ESM output
2026-03-24,Country names use formal ISO names instead of common names (11 countries),Data source (AfterShip/phone) used ISO 3166 formal names,Renamed to common names in data + enum; kept old enum keys as aliases for backward compat. BREAKING: string values changed for Vietnam/Iran/South Korea/Laos/Libya/Syria/North Macedonia/Micronesia/Tanzania/Moldova/DR Congo,Use common country names; keep old enum members as aliases
2026-03-24,IDE auto-import resolves to deep dist/types path,typesVersions only mapped literal "." path; IDEs discover deep paths,Changed typesVersions pattern to "*" wildcard + added package.json to exports,Use wildcard "*" in typesVersions to catch all deep path resolutions
